/@@ #type {import ('next') from .NextCOnfig} */
const nextConfig = {
    output: "export",
    basePath: "/jv-agile6.github.io/app/",
    images: {
        unoptimized: true,
    },
};

export default nextConfig